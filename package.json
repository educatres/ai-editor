{
  "name": "cgu-ai-text-editor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check core.js && node --check prompt-presets.js && node test.mjs",
    "serve": "python3 -m http.server 8080"
  }
}
